apiVersion: v1
kind: Service
metadata:
  name: gadget4-api
  labels:
    app: gadget4-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: gadget4-api
